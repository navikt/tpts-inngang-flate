apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: tpts-inngang-flate
  namespace: tpts
  labels:
    team: tpts
spec:
  image: {{image}}
  port: 8080
  ingresses:
    - https://tpts-inngang-flate.dev.intern.nav.no
  replicas:
    min: 1
    max: 1
  env:
    - name: CORS_DOMAIN
      value: "*"